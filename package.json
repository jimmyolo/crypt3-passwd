{
  "name": "crypt3-passwd",
  "version": "0.0.5",
  "description": "CRYPT(3)",
  "main": "index.js",
  "scripts": {
    "install": "node-gyp-build",
    "test": "tap 'test/*.test.js' --cov"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/victor0801x/crypt3-passwd.git"
  },
  "keywords": [
    "crypt3",
    "/etc/passwd",
    "/etc/shadow"
  ],
  "author": "Jimmy Huang <victor0801x@gmail.com>",
  "dependencies": {
    "node-gyp-build": "^4.2.0"
  },
  "devDependencies": {
    "node-gyp": "^6.0.1",
    "release-it": "^12.4.3",
    "tap": "^14.10.2"
  },
  "release-it": {
    "git": {
      "tagName": "v${version}",
      "commitMessage": "chore: release v${version}"
    },
    "github": {
      "release": true
    }
  }
}
